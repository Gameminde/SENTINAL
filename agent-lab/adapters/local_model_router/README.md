# Local Model Router Notes

Research focus:

- local-first inference;
- cost per run;
- latency measurement;
- fallback rules;
- accuracy vs cost tradeoff.

Sentinel target:

Use local or cheaper models for low-risk extraction and classification, then reserve stronger cloud models for high-value reasoning.
