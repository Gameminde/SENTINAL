# File Tasks

Allowed sandbox path:

```text
agent-lab/benchmarks/sandbox_workspace
```

Initial tasks:

- create a folder;
- create a markdown file;
- append a trace-like note;
- reject any write outside the sandbox path.
